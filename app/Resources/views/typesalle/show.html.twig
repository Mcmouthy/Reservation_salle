{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="center">Information type de salle</h1>

    <table class="table table-striped table-bordered table-style-delete">
        <tbody>
           {# <tr>
                <th>Id</th>
                <td>{{ typeSalle.id }}</td>
            </tr>#}
            <tr>
                <th class="center table-title">Nom</th>
                <td class="center">{{ typeSalle.nom }}</td>
            </tr>
        </tbody>
    </table>
    <div class="center div-form-style">
        <a class="btn btn-success" href="{{ path('typesalle_edit', { 'id': typeSalle.id }) }}">Modifier</a>

    </div>
    <div class="center div-form-style">

        {{ form_start(delete_form) }}
        <a class="btn btn-primary" href="{{ path('typesalle_index') }}">Retour à la liste</a>
        <input class="btn btn-warning" type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
