{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typesalle</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeSalle.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ typeSalle.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typesalle_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typesalle_edit', { 'id': typeSalle.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
