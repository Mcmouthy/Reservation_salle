{% extends 'base.html.twig' %}


{% block body %}
    <h1 class="center" >Liste utilisateurs</h1>
    <table class="table table-striped table-bordered table-style">
        <thead>
            <tr>
                <th class="center table-title">Administrateur</th>
                <th class="center table-title">Nom</th>
                <th class="center table-title">Prenom</th>
                <th class="center table-title">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for personne in personnes %}
            <tr>
                <td class="center">{% if personne.isAdmin %}Oui{% else %}Non{% endif %}</td>
                <td class="center">{{ personne.nom }}</td>
                <td class="center">{{ personne.prenom }}</td>
                <td class="center">
                    {#<ul>
                        <li>#}
                            <a class="btn btn-info btn-sm" href="{{ path('personne_show', { 'id': personne.id }) }}">show</a>
                        {#</li>
                        <li>#}
                            <a class="btn btn-warning btn-sm" href="{{ path('personne_edit', { 'id': personne.id }) }}">edit</a>
                       {# </li>
                    </ul>#}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#<ul>
        <li>#}
    <div class="center">
        <a class="btn btn-primary btn-sm" href="{{ path('personne_new') }}">Create a new personne</a>
    </div>
        {#</li>
    </ul>#}
{% endblock %}
