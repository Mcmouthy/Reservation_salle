{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="center">Création de salle</h1>
    <div class="center div-form-style">

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" type="submit" value="Créer la salle" />
        {{ form_end(form) }}
    </div>
    <div class="center div-form-style">
        <a class="btn btn-primary" href="{{ path('salle_index') }}">Retour à la liste</a>
    </div>

{% endblock %}
