{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="center">liste des réservations</h1>
    <div class="center">

        <table class="table table-striped table-bordered table-style">
            <tr>
                {#<th class="center table-title">Id</th>#}
                <th class="center table-title">Personneid</th>
                <th class="center table-title">Salleid</th>
                <th class="center table-title">Duree</th>
                <th class="center table-title">Datedebut</th>
                <th class="center table-title">Datefin</th>
                <th class="center table-title">Code</th>
                <th class="center table-title">Status</th>
                <th class="center table-title">Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for reserve in reserves %}
                <tr>
                    {#<td class="center"><a href="{{ path('reserve_show', { 'id': reserve.id }) }}">{{ reserve.id }}</a></td>#}
                    <td class="center">{{ reserve.personneId }}</td>
                    <td class="center">{{ reserve.salleId }}</td>
                    <td class="center">{{ reserve.duree }}</td>
                    <td class="center">{% if reserve.dateDebut %}{{ reserve.dateDebut|date('Y-m-d H:i:s') }}{% endif %}</td>
                    <td class="center">{% if reserve.dateFin %}{{ reserve.dateFin|date('Y-m-d H:i:s') }}{% endif %}</td>
                    <td class="center">{{ reserve.code }}</td>
                    <td class="center">{{ reserve.status }}</td>
                    <td class="center">
                        <a class="btn btn-info btn-sm" href="{{ path('reserve_show', { 'id': reserve.id }) }}">show</a>
                        <a class="btn btn-warning btn-sm" href="{{ path('reserve_edit', { 'id': reserve.id }) }}">edit</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="center div-form-style">
        <a class="btn btn-primary" href="{{ path('reserve_new') }}">Create a new reserve</a>
    </div>

{% endblock %}
